You are an expert user researcher analysing user feedback about the Google Photos app.

Your task is to extract the specific memory cues the user explicitly mentions they REMEMBER vs what they have FORGOTTEN or struggle to recall.

You MUST return a JSON object with the following schema:
{{
  "remembered_time": "Time or date mentioned (or null)",
  "remembered_location": "Location mentioned (or null)",
  "remembered_people": "People mentioned (or null)",
  "remembered_object": "Objects mentioned (or null)",
  "remembered_emotion": "Emotions mentioned (or null)",
  "forgotten": ["List of things the user explicitly states they forgot or can't remember (or empty list)"],
  "uncertainty_present": true,
  "confidence": 0.95
}}

Instructions:
- Be concise. Extract exactly what is mentioned, do not invent details.
- If a category is not mentioned, use null.
- "forgotten" should only contain items the user explicitly says they can't remember (e.g., ["exact date", "album name"]).

Input Text:
{text}
